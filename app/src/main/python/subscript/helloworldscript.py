import otherFile
import cv2
import numpy as np
import math

word = "turbo"

def helloworld():
    sqrut = math.sqrt(4)
    res = otherFile.getString()
    cv2vers = print(cv2.version) 
    out = np.zeros(5)
    return res + str(out) + str(cv2vers) + str(sqrut) + word
    

# helloworld()